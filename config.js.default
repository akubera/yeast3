/*
 *  config.js.default
 *
 *  Copy to config.js and set any parameters to setup the server
 */

var config = {};

// webserver config
config.web = {
  port : 8080,
  host : "localhost"
};

// mailing settings
config.mail = {};

// connection to the mongodb
config.db = {
    name : "tic_tac_toe",
    host : "localhost",
    port : 27017,
  secret : "database connection secret"
};

config.session_secret = 'Long session secret string';

// export the config object
module.exports = config;

